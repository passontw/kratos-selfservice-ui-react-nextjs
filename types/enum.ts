export enum request_type {
  POST,
  PUT,
  PATCH,
  DELETE,
}

export enum Navs {
  HOME = 'HOME',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  PROFILE = 'PROFILE',
  ACCOUNT = 'ACCOUNT',
  SERVICES = 'SERVICES',
  RECOVERY = 'RECOVERY',
}